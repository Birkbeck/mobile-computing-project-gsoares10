<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CulinaryCompanion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Primary brand colors -->
        <!-- Brand colors -->
        <item name="colorPrimary">@color/colorButton</item> <!-- Blush Pink -->
        <item name="colorOnPrimary">@color/colorTextSecondary</item> <!-- White -->

        <item name="colorSecondary">@color/colorSave</item> <!-- Sage Green -->
        <item name="colorOnSecondary">@color/colorTextSecondary</item> <!-- White -->

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorOnSurface">@color/colorTextPrimary</item>

        <!-- General text -->
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

    <style name="ToolbarTitleBoldStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="Theme.CulinaryCompanion" parent="Base.Theme.CulinaryCompanion" />
</resources>